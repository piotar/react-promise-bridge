---
"@piotar/react-promise-bridge": minor
---

Rewrite the README for clarity (what the library does, mental model, quick start, full API reference) and add a `SKILL.md` agent skill (compliant with the agentskills.io specification) so AI agents know how to use the package. Both `README.md` and `SKILL.md` are now included in the published package.
